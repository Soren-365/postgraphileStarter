export const query = `query getConditionalUsertabl() {
        __typename
        usertabls(condition: {,,,,,,,,, })  {
          nodes {
            id

          }
        }
      }`
      export default `${query}`
        