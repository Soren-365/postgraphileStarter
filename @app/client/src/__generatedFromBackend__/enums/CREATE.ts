export enum CREATE {
       CREATE TABLE app_public.agency (
    "id"          SERIAL primary key
,   "name"             VARCHAR(2 = 'CREATE TABLE app_public.agency (
    "id"          SERIAL primary key
,   "name"             VARCHAR(2'
,   = ''

   }