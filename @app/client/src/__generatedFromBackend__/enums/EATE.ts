export enum EATE {
       EATE TABLE app_public.roomcalendar (
    "id"    SERIAL primary key
,        "publisherId"       INT NOT NULL
,        "createdById"       INT NOT NULL
,   "isActive"          BOOLEAN DEFAULT true
,        "calendarentryId"         INT NOT NULL
,   "timeCreated"       TIMESTAMP DEFAULT NO = 'EATE TABLE app_public.roomcalendar (
    "id"    SERIAL primary key
,        "publisherId"       INT NOT NULL
,        "createdById"       INT NOT NULL
,   "isActive"          BOOLEAN DEFAULT true
,        "calendarentryId"         INT NOT NULL
,   "timeCreated"       TIMESTAMP DEFAULT NO'
,   = ''

   }